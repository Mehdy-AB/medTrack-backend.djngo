"""Django app configuration for offers."""
from django.apps import AppConfig


class OffersConfig(AppConfig):
    """Configuration for offers app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'offers'
